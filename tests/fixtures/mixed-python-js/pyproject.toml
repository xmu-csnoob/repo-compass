[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "mixed-python-js"
version = "0.1.0"
description = "Mixed Python + JS/TS repo where Python is primary"
requires-python = ">=3.9"
dependencies = ["fastapi>=0.100.0", "uvicorn>=0.23.0"]
