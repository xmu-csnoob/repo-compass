[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "python-lib"
version = "0.1.0"
description = "A sample Python library for fixture testing"
requires-python = ">=3.9"
dependencies = []
