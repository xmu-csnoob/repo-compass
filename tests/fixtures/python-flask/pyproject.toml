[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "python-flask"
version = "0.1.0"
description = "A sample Flask service for fixture testing"
requires-python = ">=3.9"
dependencies = ["flask>=3.0.0"]
