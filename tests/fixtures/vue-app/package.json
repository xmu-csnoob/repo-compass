{
  "name": "vue-app-fixture",
  "private": true,
  "scripts": {
    "dev": "genesis-ts-node --project=./tsconfig.node.json genesis.dev",
    "build": "rm -rf dist && npm run build:vue && npm run build:node",
    "build:node": "NODE_ENV=production genesis-tsc --build tsconfig.node.json",
    "build:vue": "NODE_ENV=production genesis-ts-node --project=./tsconfig.node.json genesis.build",
    "start": "NODE_ENV=production node dist/genesis.prod.js"
  },
  "dependencies": {
    "vue": "^2.7.16",
    "vue-router": "^3.6.5",
    "vuex": "^3.6.2",
    "@fmfe/genesis-core": "^0.9.0",
    "express": "^4.18.2"
  },
  "devDependencies": {
    "typescript": "^5.0.0",
    "vue-tsc": "^1.8.0"
  }
}
