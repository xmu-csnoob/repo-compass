import type {
  Comprehension,
  RepoInput,
  SignalExtraction,
} from "../contracts/index.js";

export function buildComprehension(
  _input: RepoInput,
  _signals: SignalExtraction,
): Comprehension {
  throw new Error("buildComprehension is not implemented yet");
}
