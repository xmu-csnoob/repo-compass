import type { Comprehension, ContextIndex } from "../contracts/index.js";

export function renderContextIndex(
  _comprehension: Comprehension,
): ContextIndex {
  throw new Error("renderContextIndex is not implemented yet");
}

export function renderRepoMap(_contextIndex: ContextIndex): string {
  throw new Error("renderRepoMap is not implemented yet");
}

export function renderOnboarding(_contextIndex: ContextIndex): string {
  throw new Error("renderOnboarding is not implemented yet");
}
